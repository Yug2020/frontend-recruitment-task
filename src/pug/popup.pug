article.popup(data-visible="false" onclick="popup.close(event)")
	.alert(role="alert" aria-live="assertive")
		button.alert__btn-close.btn.btn--brd0(onclick="popup.close(event)")
			| X
		h3.alert__title.title Alert!
		p.alert__mess
			| You have clicked 
			b
				span.v-count(aria-value="ss") X
				|  times 
			| to related button.
			span.alert__wrap-btn-reset.wrap-inline
				button.alert__btn-reset.btn(data-visible="false" onclick="popup.resetCount()")
					| reset